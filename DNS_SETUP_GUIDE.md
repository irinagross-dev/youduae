# 🌐 ИНСТРУКЦИЯ ПО НАСТРОЙКЕ DNS ДЛЯ SHARETRIBE

## Проблема
Не удается сделать тест DNS через консоль Sharetribe.

## ✅ Решение (по шагам)

### Шаг 1: Получите данные для DNS в Sharetribe Console

1. Откройте https://console.sharetribe.com
2. Перейдите: **Build → Domain & DNS**
3. Найдите раздел **Custom Domain** или **Domain Settings**
4. Скопируйте:
   - **CNAME target** (например: `your-marketplace.sharetribe.com` или `marketplace.sharetribe.com`)
   - **Или IP-адрес** (если используется A-запись)

### Шаг 2: Настройте DNS записи у вашего регистратора домена

**Вариант A: Использование CNAME (рекомендуется для поддомена)**

1. Войдите в панель управления вашего регистратора домена (например, GoDaddy, Namecheap, Cloudflare)
2. Перейдите в раздел **DNS Management** или **DNS Settings**
3. Добавьте **CNAME запись**:
   - **Host/Name**: `www` (или `@` если хотите основной домен)
   - **Target/Points to**: `ваш-cname-target.sharetribe.com` (из Шага 1)
   - **TTL**: `3600` (или автоматически)

**Вариант B: Использование A-записи (для основного домена)**

1. Войдите в панель управления вашего регистратора домена
2. Перейдите в раздел **DNS Management**
3. Добавьте **A запись**:
   - **Host/Name**: `@` (или оставьте пустым для основного домена)
   - **Points to/IP Address**: `IP-адрес из Sharetribe Console` (из Шага 1)
   - **TTL**: `3600`

**Вариант C: Если у вас уже есть WWW и нужно добавить основной домен**

1. Добавьте **A запись** для `@` → IP-адрес Sharetribe
2. Добавьте **CNAME запись** для `www` → ваш основной домен (или CNAME target Sharetribe)

### Шаг 3: Проверьте DNS записи через внешние инструменты

**Перед тестом в Sharetribe Console, проверьте что DNS записи распространились:**

1. **Через онлайн инструменты:**
   - https://www.whatsmydns.net/ - проверка DNS propagation
   - https://dnschecker.org/ - проверка DNS по всему миру
   - https://mxtoolbox.com/DNSLookup.aspx - детальная проверка DNS

2. **Через командную строку:**
   ```bash
   # Проверка CNAME записи
   dig www.yourdomain.com CNAME
   nslookup www.yourdomain.com
   
   # Проверка A записи
   dig yourdomain.com A
   nslookup yourdomain.com
   ```

3. **Убедитесь что:**
   - CNAME указывает на правильный домен Sharetribe
   - A запись указывает на правильный IP-адрес
   - TTL не слишком большой (3600 секунд = 1 час)

### Шаг 4: Подождите распространение DNS (DNS Propagation)

**⚠️ Важно:** DNS изменения могут занять от **15 минут до 48 часов** для полного распространения по всему миру!

**Причины задержки:**
- TTL (Time To Live) настроен на высокое значение
- DNS кеширование на разных уровнях
- Географическое распространение DNS серверов

**Что делать:**
1. Подождите минимум **1-2 часа** после изменения DNS
2. Проверьте через инструменты выше (Шаг 3)
3. Убедитесь что DNS записи видны из разных локаций

### Шаг 5: Тест DNS в Sharetribe Console

1. Вернитесь в https://console.sharetribe.com
2. Перейдите: **Build → Domain & DNS**
3. Нажмите **"Test DNS"** или **"Verify DNS"**
4. Если тест не проходит:
   - Проверьте что DNS записи распространились (Шаг 3)
   - Убедитесь что используете правильный тип записи (CNAME vs A)
   - Проверьте что нет опечаток в DNS записях
   - Подождите еще 1-2 часа

### Шаг 6: Обновите переменную окружения (если нужно)

После успешной настройки DNS, убедитесь что в `.env` файле указан правильный домен:

```bash
REACT_APP_MARKETPLACE_ROOT_URL=https://www.yourdomain.com
# или
REACT_APP_MARKETPLACE_ROOT_URL=https://yourdomain.com
```

## 🔍 Типичные проблемы и решения

### Проблема 1: "DNS test failed" в Sharetribe Console

**Причины:**
- DNS записи еще не распространились (подождите 1-2 часа)
- Использован неправильный тип записи (CNAME vs A)
- Неправильный target для CNAME
- TTL слишком большой

**Решение:**
- Проверьте DNS через внешние инструменты (Шаг 3)
- Убедитесь что используете правильный target из Sharetribe Console
- Уменьшите TTL до 3600 (1 час) для быстрого распространения

### Проблема 2: "DNS records not found"

**Причины:**
- DNS записи не добавлены
- Неправильный hostname (www vs @)
- DNS записи удалены

**Решение:**
- Проверьте что DNS записи добавлены у регистратора
- Убедитесь что используете правильный hostname
- Проверьте через `dig` или `nslookup`

### Проблема 3: "SSL certificate not ready"

**Причины:**
- Sharetribe автоматически выдает SSL сертификат после успешной проверки DNS
- Это может занять дополнительное время (до 24 часов)

**Решение:**
- Дождитесь успешной проверки DNS
- Подождите выдачу SSL сертификата (обычно автоматически)
- Проверьте статус в Sharetribe Console

### Проблема 4: Домен работает, но SSL не работает

**Причины:**
- SSL сертификат еще не выдан
- Неправильная настройка SSL в Sharetribe Console

**Решение:**
- Подождите выдачи SSL сертификата (до 24 часов)
- Убедитесь что в Sharetribe Console включен SSL
- Проверьте что используете `https://` в `REACT_APP_MARKETPLACE_ROOT_URL`

## 📝 Примеры настроек для популярных регистраторов

### GoDaddy

1. Войдите в **My Products** → **DNS**
2. В разделе **Records** добавьте:
   - **Type**: CNAME
   - **Host**: www
   - **Points to**: `your-marketplace.sharetribe.com`
   - **TTL**: 1 hour

### Namecheap

1. Войдите в **Domain List** → выберите домен → **Advanced DNS**
2. В разделе **Host Records** добавьте:
   - **Type**: CNAME Record
   - **Host**: www
   - **Value**: `your-marketplace.sharetribe.com`
   - **TTL**: Automatic

### Cloudflare

1. Войдите в **DNS** → выберите домен
2. Добавьте запись:
   - **Type**: CNAME
   - **Name**: www
   - **Target**: `your-marketplace.sharetribe.com`
   - **Proxy status**: DNS only (серый облачок)
   - **TTL**: Auto

## 🔗 Полезные ссылки

- [Sharetribe Domain Setup Guide](https://www.sharetribe.com/docs/guides/domain-setup/)
- [DNS Propagation Checker](https://www.whatsmydns.net/)
- [Sharetribe Support](https://www.sharetribe.com/support/)

## ⚠️ Важные замечания

1. **Не используйте Cloudflare Proxy** (оранжевый облачок) для Sharetribe - это может вызвать проблемы с SSL
2. **TTL должен быть разумным** - 3600 секунд (1 час) оптимально для изменений
3. **DNS propagation может занять время** - не паникуйте, подождите 1-2 часа
4. **Проверяйте DNS через внешние инструменты** перед тестом в Sharetribe Console
5. **Используйте правильный тип записи** - CNAME для поддоменов, A для основного домена

## 📞 Если проблема не решается

1. Проверьте DNS записи через внешние инструменты (Шаг 3)
2. Убедитесь что DNS записи распространились (подождите 1-2 часа)
3. Обратитесь в поддержку Sharetribe: https://www.sharetribe.com/support/
4. Проверьте документацию Sharetribe: https://www.sharetribe.com/docs/

