# 📋 Счётчик завершённых заданий для Provider (Заказчик)

## Обзор

Для пользователей с ролью **Provider (Заказчик)** счётчик "Завершённые задания" отображает количество **закрытых листингов** (собственных заданий).

## 🔧 Логика работы

### Для Provider (customer: true):
```javascript
completedTasks = closedListings.length
```

Где `closedListings` — это листинги со статусом `state === 'closed'`.

### Для Customer (provider: true):
```javascript
completedTasks = completedTransactions.length
```

Где `completedTransactions` — завершённые транзакции, где пользователь был исполнителем.

## 📊 Отображение

### В статистике профиля:
```
┌─────────────────────────────────┐
│ Количество предложенных заданий │ ← для Provider
│              42                  │
└─────────────────────────────────┘
```

### В списке заданий:
```
Завершённые задания (5)
┌──────────────────┐ ┌──────────────────┐
│  Листинг 1       │ │  Листинг 2       │
│  [CLOSED]        │ │  [CLOSED]        │
└──────────────────┘ └──────────────────┘
```

## 🎯 Когда листинг считается "закрытым"?

Листинг переходит в статус `closed` когда:
1. ✅ Заказчик (Provider) **закрывает задание** после выполнения
2. ✅ Транзакция завершена (transition: `transition/complete`)
3. ✅ Листинг помечен как завершённый в системе

## 📝 Переводы

### Русский (`ru.json`):
- `ProfilePage.proposedTasks`: "Количество предложенных заданий"
- `ProfilePage.completedListingsTitle`: "Завершённые задания ({count})"

### Английский (`en.json`):
- `ProfilePage.proposedTasks`: "Proposed tasks"
- `ProfilePage.completedListingsTitle`: "Completed tasks ({count})"

## 🔍 Debug информация

Добавлены debug логи для отслеживания:

```javascript
console.log('📊 Profile listings:', {
  totalListings: listings.length,
  openListings: openListings.length,
  closedListings: closedListings.length,
  userTypeRoles,
  allStates: [...],
});

console.log('📊 ListingOwnerStats:', {
  userTypeRoles,
  completedTaskCount,
  reviewsOfListingOwner: reviewsOfListingOwner.length,
  finalCompletedTasks: completedTasks,
  isPureProvider,
});
```

## 🚀 Проверка

### Как проверить на локальном сервере:

1. Откройте профиль Provider: `http://localhost:3000/u/{providerId}`
2. Откройте консоль браузера (F12)
3. Найдите логи `📊 Profile listings:`
4. Убедитесь, что:
   - `closedListings` > 0 (если есть закрытые задания)
   - `userTypeRoles.customer === true`
   - Счётчик отображается правильно

### Пример лога:
```
📊 Profile listings: {
  totalListings: 10,
  openListings: 5,
  closedListings: 5,  ← это значение должно отображаться в счётчике
  userTypeRoles: { customer: true, provider: false },
  allStates: ['published', 'published', 'closed', 'closed', ...]
}
```

## ⚠️ Возможные проблемы

### Проблема: Счётчик показывает 0
**Причины:**
1. У пользователя нет закрытых листингов
2. Листинги не загружаются (ошибка API)
3. Неправильная роль пользователя

**Решение:**
- Проверьте логи в консоли
- Убедитесь, что `userTypeRoles.customer === true`
- Проверьте, что листинги загружены: `listings.length > 0`

### Проблема: Листинг не переходит в статус "closed"
**Причины:**
1. Транзакция не завершена полностью
2. Неправильный процесс транзакции

**Решение:**
- Проверьте процесс транзакции в Sharetribe Console
- Убедитесь, что transition `transition/complete` выполнен
- Проверьте, что листинг обновился в базе данных

## 📁 Связанные файлы

- `src/containers/ProfilePage/ProfilePage.js` — основная логика
- `src/containers/ProfilePage/ProfilePage.duck.js` — загрузка листингов
- `src/translations/ru.json` — русские переводы
- `src/translations/en.json` — английские переводы
- `COMPLETED_TASKS_UI.md` — документация по UI

## ✅ Итого

Счётчик завершённых заданий для Provider работает на основе:
- **Источник данных**: `closedListings.length`
- **Фильтр**: `state === 'closed'`
- **Отображение**: адаптивный UI с градиентами для больших чисел
- **Переводы**: Ru/En

