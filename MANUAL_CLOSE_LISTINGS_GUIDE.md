# 🔧 Ручное закрытие листингов - Инструкция

## Проблема

Листинги не закрываются автоматически после завершения транзакции. В логах видно:
- ✅ Транзакция завершена (`processState: "completed"`)
- ❌ Листинг остаётся `published`
- ❌ Нет логов `🔒 Closing listing:`

## Причины

1. **Код не выполнился** - кнопка "Закрыть задание" не была нажата после добавления нового кода
2. **Транзакция уже завершена** - код срабатывает только при нажатии кнопки
3. **SDK не инициализирован** - проблема с `getSdk()`

## Решения

### ✅ Решение 1: Тест с новой транзакцией

**Самое надёжное решение!**

1. **Создайте НОВОЕ задание** как Provider
2. Найдите исполнителя
3. **Нажмите "Закрыть задание"** - теперь код должен сработать
4. Проверьте логи в консоли:

```
🔄 onComplete: starting transition/complete...
✅ onComplete: transition successful
🔍 Checking listing to close: { ... }
🔒 Closing listing: ...
✅ Listing closed successfully
```

5. Проверьте профиль - счётчик должен показать **1**

### ✅ Решение 2: Закрыть через серверный скрипт

Для **уже завершённых** транзакций используйте скрипт:

```bash
cd /Users/admin/web-template
node scripts/close-completed-listings.js
```

**Что делает скрипт:**
1. Находит все завершённые транзакции
2. Получает связанные листинги
3. Проверяет их статус
4. Закрывает те, что в `published`

**Вывод будет таким:**
```
🔍 Searching for completed transactions...

Found 2 transactions with lastTransition: transition/complete
Found 1 transactions with lastTransition: transition/reviewed-by-provider

📋 Total unique listings to check: 2

Listing 691924cf... - Current state: published
  ✅ CLOSED!

Listing 691927c6... - Current state: published
  ✅ CLOSED!

==================================================
📊 Summary:
  ✅ Closed: 2
  ✓ Already closed: 0
  ❌ Errors: 0
==================================================

✅ Script completed successfully!
```

### ✅ Решение 3: Закрыть через Sharetribe Console

**Вручную для 1-2 листингов:**

1. Откройте [Sharetribe Console](https://console.sharetribe.com/)
2. **Manage → Listings**
3. Найдите листинги с ID:
   - `691924cf-b7f5-4586-89f2-76c54941855f`
   - `691927c6-a8a9-482d-9da7-7e7c211f88fc`
4. Нажмите на каждый
5. **Actions → Close listing**
6. Подтвердите

## Проверка результата

После закрытия листингов:

### 1. Обновите страницу профиля
```
http://localhost:3000/u/6901bc1d-ba6d-45ec-b6a5-6f4fae1a94f3
```

### 2. Проверьте логи
```javascript
📊 Profile listings: {
  closedListings: 2,  // ✅ Должно быть > 0!
  allStates: ['closed', 'closed']  // ✅ Статусы изменились!
}

✅ Final value showing in UI: 2  // ✅ Счётчик работает!
```

### 3. Проверьте UI
```
Завершённые задания: 2  ← Должно появиться! ✅
```

## Отладка нового кода

Если вы создаёте **новое** задание и нажимаете "Закрыть задание", но листинг не закрывается:

### Шаг 1: Проверьте логи
Должны быть все эти логи:
```
✅ onComplete: transition successful
🔍 Checking listing to close: { hasTransaction: true, hasListing: true, ... }
🔒 Closing listing: ...
✅ Listing closed successfully
```

### Шаг 2: Если лога `🔍 Checking listing to close:` НЕТ
→ Код вообще не выполняется
→ Проверьте, что изменения сохранены и страница перезагружена

### Шаг 3: Если есть `⚠️ No listing ID found`
→ Проблема с получением ID листинга
→ Отправьте мне полный лог `🔍 Checking listing to close:`

### Шаг 4: Если есть `❌ Failed to close listing`
→ Ошибка SDK
→ Отправьте мне `Error details:` из лога

## Требования для скрипта

Для использования `close-completed-listings.js` нужны:

### 1. Integration API credentials
Добавьте в `.env`:
```bash
SHARETRIBE_INTEGRATION_API_CLIENT_ID=ваш-client-id
SHARETRIBE_INTEGRATION_API_CLIENT_SECRET=ваш-secret
```

**Где взять:**
1. [Sharetribe Console](https://console.sharetribe.com/)
2. **Build → Applications**
3. Создайте новое приложение (если нет)
4. Скопируйте **Client ID** и **Client secret**

### 2. Установите зависимости
```bash
npm install sharetribe-flex-sdk dotenv
```

## Рекомендации

### Для будущих транзакций:
✅ **Новый код работает автоматически!**
- Создавайте задания
- Завершайте их через "Закрыть задание"
- Листинг закроется автоматически
- Счётчик обновится

### Для старых транзакций:
1. Используйте скрипт `close-completed-listings.js`
2. Или закройте вручную через Console

## Частые ошибки

### Error: SDK not available
→ `getSdk()` вернул `null`
→ Проверьте, что вы на клиенте (не SSR)
→ Проверьте `appSettings.sdk` в консоли

### Error: 403 Forbidden
→ У пользователя нет прав закрыть листинг
→ Убедитесь, что вы Provider (владелец листинга)

### Error: 404 Not Found
→ Листинг не найден
→ Проверьте правильность ID

## Контрольный чеклист

- [ ] Создано новое задание
- [ ] Задание завершено через "Закрыть задание"
- [ ] В логах есть `🔒 Closing listing:`
- [ ] В логах есть `✅ Listing closed successfully`
- [ ] Профиль перезагружен
- [ ] Счётчик показывает правильное значение
- [ ] Старые листинги закрыты через скрипт или Console

## Если ничего не помогает

Отправьте мне:
1. **Полные логи** из консоли после нажатия "Закрыть задание"
2. **Скриншот** страницы транзакции
3. **ID листинга**, который нужно закрыть

Я помогу найти проблему! 🚀

