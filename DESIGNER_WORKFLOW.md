# 🎨 Git Workflow для дизайнера

Привет, Irina! Это инструкция по работе с Git для проекта YouDo.ae.

---

## 🚀 Первичная настройка (один раз)

### 1. Клонировать проект (если еще не сделано):
```bash
git clone https://github.com/Magmalena-maker/youdoae.git
cd youdoae
```

### 2. Установить зависимости:
```bash
yarn install
```

### 3. Настроить .env файл:
```bash
# Создать .env из template
cp .env-template .env

# Открыть .env и вставить ключи (получить у Magmalena):
# REACT_APP_SHARETRIBE_SDK_CLIENT_ID=...
# SHARETRIBE_SDK_CLIENT_SECRET=...
```

### 4. Настроить Git (если еще не настроен):
```bash
git config --global user.name "Irina Gross"
git config --global user.email "iagross@icloud.com"
```

### 5. Запустить проект:
```bash
yarn dev
```

Откройте http://localhost:3000

---

## 📅 Ежедневная работа

### Шаг 1: Получить последние изменения
**ВСЕГДА начинайте с этого!**

```bash
git pull origin main
```

### Шаг 2: Работать над дизайном
- Редактируйте CSS файлы
- Обновляйте компоненты
- Добавляйте изображения
- Проверяйте в браузере (http://localhost:3000)

### Шаг 3: Проверить изменения
```bash
git status
```

Вы увидите список измененных файлов:
```
modified:   src/containers/PageBuilder/SectionHero.module.css
modified:   src/assets/main-bg.png
```

⚠️ **Важно:** Если видите `.env` в списке - **НЕ добавляйте его!**

### Шаг 4: Добавить изменения
```bash
# Добавить ВСЕ измененные файлы:
git add .

# ИЛИ добавить конкретные файлы:
git add src/containers/PageBuilder/SectionHero.module.css
git add src/assets/main-bg.png
```

### Шаг 5: Закоммитить
```bash
git commit -m "design: краткое описание изменений"
```

**Примеры хороших commit сообщений:**
```bash
git commit -m "design: Update hero section background"
git commit -m "design: Improve mobile layout for homepage"
git commit -m "design: Add new brand colors to CSS"
git commit -m "design: Fix button spacing on desktop"
```

### Шаг 6: Отправить на GitHub
```bash
git push origin main
```

---

## 🆘 Частые ситуации

### ❌ Ошибка: "Updates were rejected"
**Причина:** Кто-то (Magmalena) отправил изменения раньше вас.

**Решение:**
```bash
git pull origin main
# Если есть конфликты - разрешите их
git push origin main
```

### ❌ Ошибка: "conflict in file.css"
**Причина:** Вы и Magmalena редактировали один и тот же файл.

**Решение:**
1. Откройте файл с конфликтом
2. Найдите строки с `<<<<<<<`, `=======`, `>>>>>>>`
3. Выберите нужную версию или объедините обе
4. Удалите маркеры конфликта
5. Сохраните файл
6. Выполните:
```bash
git add файл-с-конфликтом.css
git commit -m "Resolve merge conflict"
git push origin main
```

### ⚠️ Случайно добавили .env в коммит
**НЕ ПАНИКУЙТЕ!**

Если еще НЕ сделали `git push`:
```bash
git reset HEAD .env
```

Если уже сделали `git push` - немедленно сообщите Magmalena!

---

## ✅ Чеклист перед push

- [ ] `git pull origin main` - получили последние изменения
- [ ] Проверили, что все работает локально (yarn dev)
- [ ] `git status` - проверили список изменений
- [ ] **НЕТ .env в списке изменений!**
- [ ] `git add .` - добавили файлы
- [ ] `git commit -m "..."` - закоммитили с понятным сообщением
- [ ] `git push origin main` - отправили на GitHub

---

## 🔒 Безопасность

### ✅ МОЖНО коммитить:
- CSS файлы
- JavaScript/React компоненты
- Изображения
- Переводы (en.json, ru.json)
- Документацию (.md файлы)

### ❌ НЕЛЬЗЯ коммитить:
- `.env` файлы (с API ключами)
- `node_modules/` (зависимости)
- `build/` (собранные файлы)
- Личные заметки с паролями

---

## 🤝 Координация с Magmalena

### Перед началом большой задачи:
1. Напишите Magmalena: "Начинаю работу над X"
2. Это поможет избежать конфликтов

### После завершения задачи:
1. Сделайте `git push`
2. Напишите Magmalena: "Запушила изменения в X"

---

## 📞 Помощь

Если что-то не работает:
1. Проверьте вывод команды - там часто есть подсказки
2. Напишите Magmalena с описанием проблемы
3. Отправьте screenshot ошибки

---

## 🎉 Готово!

Теперь вы можете безопасно работать над дизайном и отправлять изменения! 

**Важно помнить:**
- Всегда начинайте с `git pull`
- Проверяйте `git status` перед коммитом
- Никогда не коммитьте `.env` файлы
- Пишите понятные commit сообщения

Удачи! 🚀

