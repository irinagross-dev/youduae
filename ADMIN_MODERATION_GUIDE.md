# 📋 Руководство администратора по модерации листингов

## Обзор системы

Provider (заказчики) могут видеть статус модерации своих листингов на странице **"Мои задания"**.

Уведомления о модерации отображаются:
- ✅ **Зеленый бейдж** = Листинг одобрен
- ❌ **Красный бейдж** = Листинг отклонен (с причиной)

Статусы листингов (определяются автоматически):
- 🟢 **"В работе"** = Исполнитель выбран (после accept-offer)
- ⚫ **"Закрыт"** = Работа завершена (после complete/review)

---

## Как модерировать листинги

### 1. Открыть Sharetribe Console

```
https://console.sharetribe.com/
```

Войдите в свой аккаунт администратора.

### 2. Найти листинг

```
Manage → Listings → Выберите нужный листинг
```

### 3. Одобрить листинг

В секции **"Public Data"**:

1. Нажмите **"+ Add new field"**
2. Заполните:
   - Key: `moderationStatus`
   - Type: `String`
   - Value: `approved`
3. Нажмите галочку (Add)
4. Нажмите **"Save changes"** внизу страницы

**Результат:** Provider увидит зеленый бейдж "✅ Листинг одобрен"

### 4. Отклонить листинг

В секции **"Public Data"**:

1. Добавьте первое поле:
   - Key: `moderationStatus`
   - Type: `String`
   - Value: `rejected`
   - Нажмите галочку (Add)

2. Добавьте второе поле:
   - Key: `moderationReason`
   - Type: `String`
   - Value: `Недостаточно информации о проекте`
   - Нажмите галочку (Add)

3. Нажмите **"Save changes"** внизу страницы

**Результат:** Provider увидит красный бейдж "❌ Листинг отклонен: Недостаточно информации о проекте"

---

## 🤖 Автоматическое определение статусов

**Статусы листингов теперь определяются АВТОМАТИЧЕСКИ!**

Вам не нужно вручную устанавливать статусы. Система определяет их на основе транзакций:

- **"В работе"** - появляется автоматически после `transition/accept-offer`
- **"Закрыт"** - появляется автоматически после `transition/complete` или отзывов

**Ничего делать не нужно!** Просто модерируйте листинги (одобряйте/отклоняйте).

---

## Типичные сценарии

### Сценарий 1: Новый листинг создан

1. Provider создал листинг
2. Админ просматривает в Console
3. Админ устанавливает `moderationStatus: "approved"`
4. Provider видит уведомление об одобрении

### Сценарий 2: Листинг не соответствует требованиям

1. Админ находит проблему в листинге
2. Админ устанавливает:
   ```json
   {
     "moderationStatus": "rejected",
     "moderationReason": "Не указана цена услуги"
   }
   ```
3. Provider видит уведомление с причиной отклонения
4. Provider исправляет листинг
5. Админ повторно проверяет и одобряет

### Сценарий 3: Листинг выполняется

1. Customer выбрал исполнителя (transition/accept-offer)
2. **Система автоматически** показывает бейдж "В работе"
3. Provider видит статус "В работе" на своей странице "Мои задания"

### Сценарий 4: Листинг завершен

1. Работа выполнена (transition/complete)
2. **Система автоматически** показывает бейдж "Закрыт"
3. Provider видит статус "Закрыт" на своей странице "Мои задания"

---

## Примеры полей в Public Data

### Одобренный листинг

После добавления полей вы увидите:
```
moderationStatus: "approved"
```
*(Статус "В работе" определяется автоматически по транзакциям)*

### Отклоненный листинг

После добавления полей вы увидите:
```
moderationStatus: "rejected"
moderationReason: "Некорректное описание услуги"
```

### Новый листинг (ожидает модерации)

Public Data не содержит поля `moderationStatus`  
*(Уведомление не отображается до установки moderationStatus)*

---

## Важные замечания

1. **moderationStatus** - ключевое поле модерации
   - Если не установлен или равен `"pending"`, уведомление не показывается
   - Значения: `"approved"`, `"rejected"`, `"pending"`

2. **moderationReason** обязателен для отклонения
   - Всегда указывайте причину при `moderationStatus: "rejected"`
   - Помогает Provider понять, что нужно исправить

3. **Статусы определяются автоматически**
   - "В работе" - после выбора исполнителя
   - "Закрыт" - после завершения работы
   - Вам не нужно вручную устанавливать эти статусы!

4. **Provider видит изменения сразу**
   - После сохранения в Console
   - Требуется обновить страницу "Мои задания"

---

## Проверка работы системы

### Тестирование модерации

1. Создайте тестовый листинг как Provider
2. В Console добавьте:
   ```json
   {
     "moderationStatus": "approved"
   }
   ```
3. Откройте "Мои задания" как Provider
4. Убедитесь, что видите зеленый бейдж

### Тестирование статусов

1. В Console добавьте:
   ```json
   {
     "status": "in-progress"
   }
   ```
2. Обновите страницу "Мои задания"
3. Убедитесь, что видите бейдж "В работе" в заголовке

---

## FAQ

**Q: Где Provider видит уведомления?**
A: На странице "Мои задания" (кнопка в топбаре), под каждым листингом, рядом с кнопкой "Edit listing"

**Q: Можно ли скрыть уведомление после просмотра?**
A: Сейчас нет, но можно удалить `moderationStatus` из Public Data в Console

**Q: Как изменить причину отклонения?**
A: Просто измените значение `moderationReason` в Public Data

**Q: Что делать, если Provider не видит уведомление?**
A: Проверьте:
   1. Установлен ли `moderationStatus` в Public Data
   2. Provider открыл страницу "Мои задания"
   3. Provider обновил страницу после изменений

---

**Система готова к использованию! Модерируйте листинги через Sharetribe Console.**

