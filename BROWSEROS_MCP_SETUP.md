# 🌐 BrowserOS MCP Configuration для Cursor

## Что такое BrowserOS MCP?

BrowserOS MCP (Model Context Protocol) - это сервер, который позволяет AI ассистенту (мне, Claude) автоматизировать действия в браузере через Puppeteer. Это даёт возможность:

- 🔍 Автоматически тестировать веб-приложения
- 📸 Делать скриншоты страниц
- 🤖 Автоматизировать рутинные задачи в браузере
- ✅ Проводить E2E тестирование
- 🔧 Парсить данные с веб-сайтов

## 📋 Шаг 1: Убедитесь, что BrowserOS установлен

Если ещё не установлен, выполните:

```bash
npm install -g @cloud101/mcp-browseros
```

**ИЛИ** (если пакет недоступен, используйте Puppeteer MCP):

```bash
npm install -g puppeteer-mcp-server
```

## 🔧 Шаг 2: Настройка в Cursor

### Для macOS/Linux:

Создайте или отредактируйте файл:
```
~/.cursor/mcp.json
```

**ИЛИ** (альтернативный путь):
```
~/Library/Application Support/Cursor/User/globalStorage/mcp.json
```

### Для Windows:

```
%APPDATA%\Cursor\User\globalStorage\mcp.json
```

## 📝 Шаг 3: Добавьте конфигурацию

Вставьте следующий JSON в файл `mcp.json`:

```json
{
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": [
        "-y",
        "puppeteer-mcp-server"
      ],
      "env": {
        "PUPPETEER_HEADLESS": "true"
      }
    }
  }
}
```

**ИЛИ** если у вас установлен BrowserOS:

```json
{
  "mcpServers": {
    "browseros": {
      "command": "npx",
      "args": [
        "-y",
        "@cloud101/mcp-browseros"
      ],
      "env": {}
    }
  }
}
```

## 🎛️ Дополнительные параметры

Вы можете настроить дополнительные параметры через `env`:

```json
{
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "puppeteer-mcp-server"],
      "env": {
        "PUPPETEER_HEADLESS": "false",  // Показывать браузер (полезно для отладки)
        "PUPPETEER_SLOWMO": "100",      // Замедлить действия на 100ms
        "PUPPETEER_TIMEOUT": "30000"    // Таймаут 30 секунд
      }
    }
  }
}
```

## 🔄 Шаг 4: Перезапустите Cursor

После добавления конфигурации:

1. Закройте Cursor полностью (`Cmd + Q` на Mac)
2. Откройте Cursor снова
3. Я смогу использовать команды для работы с браузером!

## ✅ Проверка работы

После настройки я смогу:

- Открывать веб-страницы
- Делать скриншоты
- Заполнять формы
- Кликать по элементам
- Извлекать данные со страниц

## 🔍 Альтернатива: Использование официального Puppeteer MCP

Если `@cloud101/mcp-browseros` не работает, используйте официальный Puppeteer MCP:

```bash
npm install -g @modelcontextprotocol/server-puppeteer
```

Конфигурация:

```json
{
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ]
    }
  }
}
```

## 📚 Примеры использования

После настройки вы сможете попросить меня:

1. **Тестирование**: "Открой localhost:3000 и проверь, что кнопка входа работает"
2. **Скриншоты**: "Сделай скриншот главной страницы"
3. **Автоматизация**: "Зарегистрируй тестового пользователя на сайте"
4. **Парсинг**: "Собери все цены с этой страницы"

## 🆘 Устранение неполадок

### MCP сервер не найден

Убедитесь, что:
- Файл `mcp.json` создан в правильной директории
- JSON синтаксис корректен
- Cursor перезапущен

### Puppeteer не устанавливается

```bash
# Установите явно с Chromium
npm install -g puppeteer --unsafe-perm=true
```

### Порты заняты

Если порт 9222 (Chrome DevTools) занят:

```bash
lsof -ti:9222 | xargs kill -9
```

---

**💡 Совет**: Начните с `PUPPETEER_HEADLESS: "false"` чтобы видеть, что происходит в браузере при отладке.


