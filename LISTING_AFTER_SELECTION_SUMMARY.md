# Завершение логики листинга после выбора исполнителя

## ✅ Реализованные функции

### 1. Скрытие формы отклика после accept-offer ✓

**Файлы изменены:**
- `src/containers/ListingPage/ListingPageCarousel.js`

**Логика:**
```javascript
const listingInProgress = listingStatus === 'in-progress';
const isHired = publicData.hired === true;

const canShowOfferForm =
  !isOwner && hasListing && isPublished && isAssignmentProcess && !listingInProgress && !isHired;
```

**Результат:** 
- После принятия отклика форма автоматически скрывается для всех пользователей
- Используется двойная проверка: backend статус + publicData.hired

---

### 2. Кнопка "Отклонить" для невыбранных откликов ✓

**Файлы изменены:**
- `src/components/OfferList.jsx`
- `src/components/OfferList.module.css`

**UI:**
```
[Выбрать исполнителя] [Отклонить]
```

**Функционал:**
- Кнопка красного цвета с подтверждением
- Вызывает `transition/decline-offer`
- Отклонённые отклики больше не отображаются

**CSS стили:**
```css
.declineButton {
  background-color: #f44336;
  color: white;
  /* hover: #d32f2f */
}
```

---

### 3. Новый переход decline-offer в процессе ✓

**Файлы изменены:**
- `src/transactions/transactionProcessAssignment.js`

**Добавлено:**
```javascript
transitions.DECLINE_OFFER = 'transition/decline-offer';
states.DECLINED = 'declined';

graph:
  [inquiry] --decline-offer--> [declined] (final)
```

**Требуется в Sharetribe Console:**
```clojure
{:name :transition/decline-offer,
 :actor :actor.role/customer,
 :actions [],
 :from :state/inquiry,
 :to :state/declined,
 :privileged? true}
```

---

### 4. Обновление publicData.hired при accept-offer ✓

**Требуется в Sharetribe Console:**

Обновить `transition/accept-offer` в `process.edn`:

```clojure
{:name :transition/accept-offer,
 :actor :actor.role/customer,
 :actions [{:name :action/update-protected-data}
           {:name :action/update-listing,
            :config {:listingFields [[:publicData [:hired] "hired=true"]]}}],
 :to :state/accepted,
 :privileged? true}
```

**Результат:** Листинг автоматически помечается как "нанятый" при принятии отклика.

---

### 5. Улучшенные статусы на карточках листингов ✓

**Файлы изменены:**
- `src/translations/ru.json`
- `src/translations/en.json`

**Добавлены переводы:**
- `ManageListingCard.inProgress`: "В работе" / "In Progress"
- `ManageListingCard.closed`: "Закрыт" / "Closed"
- `ManageListingCard.newOffers`: "Новые отклики" / "New Offers"
- `ManageListingCard.waitingForReview`: "Ожидает отзыв" / "Waiting for Review"
- `ManageListingCard.completed`: "Завершено" / "Completed"

**UI на ManageListingCard:**
```
[Название листинга] 🟢 В работе
[Название листинга] ⚫ Закрыт
```

---

### 6. Проверка прав доступа для OfferList ✓

**Файл:** `src/components/OfferList.jsx`

**Логика:**
```javascript
if (!isOwner) return null; // Только владелец видит отклики

// Также проверка в useEffect:
if (!listingId || !isOwner) {
  setLoading(false);
  return;
}
```

**Безопасность:**
- Frontend проверка: `isOwner`
- Backend проверка: privileged transitions
- `protectedData.offer` видна только участникам сделки

---

### 7. Email уведомления для отклонённых офферов ✓

**Документация:** `EMAIL_NOTIFICATIONS_GUIDE.md`

**Шаблон email:**
- Subject: "Ваш отклик не был принят"
- Содержит ссылку на поиск других заданий
- Советы по улучшению профиля

**Настройка в Console:**
```clojure
{:name :notification/offer-declined,
 :on :transition/decline-offer,
 :to :actor.role/provider,
 :template :offer-declined}
```

---

## 📊 Диаграмма процесса

```
[initial]
    ↓
    inquire (Provider) → [inquiry] ←─ Отклики от исполнителей
                            │
                            ├─→ accept-offer (Customer)
                            │   ├── update publicData.hired=true
                            │   ├── update listing status=in-progress
                            │   └── send email to provider ✓
                            │   
                            │   → [accepted] → complete → [completed] → reviews
                            │
                            └─→ decline-offer (Customer)
                                ├── send email to provider ✉
                                └── → [declined] ✓
```

---

## 🔒 Безопасность данных

### Проверки прав

| Компонент | Проверка | Уровень |
|-----------|----------|---------|
| OfferList | `isOwner` | Frontend |
| OfferForm | `!isOwner && !isHired` | Frontend |
| accept-offer | `actor.role/customer` | Backend (privileged) |
| decline-offer | `actor.role/customer` | Backend (privileged) |
| protectedData.offer | relationship | Sharetribe API |

### Защита данных

- **Цена и комментарий**: `protectedData.offer` - видны только участникам
- **Список откликов**: Только владелец листинга (через privileged API)
- **Действия по сделке**: Только актёры транзакции в нужных состояниях

---

## 📱 Навигация и разделы аккаунта

### Inbox для Provider

**Вкладки:**
- **Sales** (Продажи) - Активные задания, где Provider выбран исполнителем
- Показывает статус: "Новые отклики", "В работе", "Ожидает отзыв", "Завершено"

### Inbox для Customer

**Вкладки:**
- **Orders** (Заказы) - Задания, где Customer выбран исполнителем другими

### Manage Listings

**Для Provider:**
- Список всех своих листингов
- Бейджи статуса рядом с названием:
  - 🟢 "В работе"
  - ⚫ "Закрыт"
  - ⏳ "Новые отклики" (есть непринятые отклики)

---

## 🎯 Поведение листинга после выбора

### Для владельца листинга (Customer)

✅ **Видит:**
- Принятый отклик с бейджем "✅ Исполнитель выбран"
- Кнопку "Открыть чат" для общения с исполнителем
- Статус "В работе" на карточке листинга
- Остальные отклики с кнопкой "Отклонить"

❌ **Не видит:**
- Форму создания нового отклика (она скрыта)

### Для других пользователей (не владельцев)

✅ **Видят:**
- Листинг в списке с бейджем "В работе"
- Сообщение: "Исполнитель выбран, задание в процессе выполнения"

❌ **Не видят:**
- Форму отклика (скрыта через `!canShowOfferForm`)
- Список откликов (доступен только владельцу)

### Для отклонённых Provider

✅ **Получают:**
- Email уведомление "Ваш отклик не был принят"
- Советы по улучшению профиля
- Ссылку на поиск других заданий

❌ **Не могут:**
- Больше откликнуться на это задание
- Видеть транзакцию в активных

---

## 📋 Пошаговое тестирование

### Тест 1: Принятие отклика

1. **Provider** создаёт отклик через форму
2. **Customer** (владелец) видит отклик в списке
3. **Customer** нажимает "Выбрать исполнителя"
4. ✅ **Результат:**
   - Форма отклика скрыта для всех
   - Статус листинга = "В работе"
   - `publicData.hired = true`
   - Provider получает email уведомление

### Тест 2: Отклонение отклика

1. **Provider1** и **Provider2** создают отклики
2. **Customer** нажимает "Выбрать" для Provider1
3. **Customer** нажимает "Отклонить" для Provider2
4. ✅ **Результат:**
   - Provider2 получает email "Отклик не принят"
   - Транзакция Provider2 в состоянии `declined`
   - Provider2 больше не видит этот отклик в активных

### Тест 3: Скрытие формы

1. Создать листинг
2. Принять один отклик
3. Открыть листинг в incognito mode
4. ✅ **Результат:**
   - Форма отклика не отображается
   - Показан баннер "Исполнитель выбран"

---

## 📚 Документация

Созданные гайды:
1. `ASSIGNMENT_PROCESS_UPDATE_GUIDE.md` - Обновление процесса в Console
2. `EMAIL_NOTIFICATIONS_GUIDE.md` - Настройка email уведомлений
3. `LISTING_AFTER_SELECTION_SUMMARY.md` (этот файл) - Итоговая документация

---

## 🚀 Следующие шаги для деплоя

### 1. Обновить process.edn в Sharetribe Console

Следуйте инструкциям в `ASSIGNMENT_PROCESS_UPDATE_GUIDE.md`:
- Добавить `transition/decline-offer`
- Обновить `transition/accept-offer` с `action/update-listing`
- Добавить состояние `declined`

### 2. Создать email шаблон

Следуйте инструкциям в `EMAIL_NOTIFICATIONS_GUIDE.md`:
- Создать шаблон `offer-declined`
- Добавить notification в процесс

### 3. Протестировать

1. Создать тестовое задание
2. Отправить несколько откликов
3. Принять один отклик
4. Отклонить остальные
5. Проверить:
   - Email уведомления
   - Скрытие формы
   - Статусы на карточках

### 4. Деплой frontend

```bash
git add -A
git commit -m "feat: Complete listing behavior after offer selection"
git push origin main
```

---

## ⚙️ Технические детали

### Backend API endpoints

- `POST /api/initiate-privileged` - Создание отклика (inquire)
- `POST /api/transition-privileged` - Transitions (accept-offer, decline-offer)
- `GET /api/listing-status` - Получение статуса листинга
- `POST /api/listing-status/update` - Обновление статуса листинга

### Frontend компоненты

- `OfferForm.jsx` - Форма создания отклика
- `OfferList.jsx` - Список откликов с кнопками принять/отклонить
- `ListingPageCarousel.js` - Логика отображения формы
- `ManageListingCard.js` - Карточка листинга с статусом

### Процесс транзакций

- `transactionProcessAssignment.js` - Frontend описание процесса
- `ext/transaction-processes/assignment-flow-v3/` - Локальная копия процесса

---

## 🎓 Полезные ссылки

- [Sharetribe Transaction Process](https://www.sharetribe.com/docs/howto/edit-transaction-process-with-flex-cli/)
- [Sharetribe Actions](https://www.sharetribe.com/docs/references/transaction-process-actions/)
- [Sharetribe Email Templates](https://www.sharetribe.com/docs/references/email-templates/)
- [Sharetribe Notifications](https://www.sharetribe.com/docs/references/transaction-process-format/#notifications)




