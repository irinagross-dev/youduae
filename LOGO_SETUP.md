# 🎨 Инструкция по замене временного логотипа на свое изображение

## Текущее состояние

**Сейчас используется временный SVG-логотип** (буквы Y и D на белом фоне). Сайт полностью работает!

Чтобы заменить его на свое изображение, следуйте инструкциям ниже.

---

## 📂 Шаг 1: Добавьте файл Logo.png

Файл изображения логотипа должен быть размещен по следующему пути:

```
/Users/admin/web-template/src/assets/Logo.png
```

### Требования к изображению:
- **Формат**: PNG (рекомендуется с прозрачным фоном)
- **Размер**: 60x60 пикселей (или больше для лучшего качества на Retina-дисплеях, например 120x120px)
- **Название файла**: `Logo.png` (с заглавной буквы L)

---

## ⚙️ Шаг 2: Активируйте использование изображения

Откройте файл:
```
/Users/admin/web-template/src/containers/TopbarCustom/TopbarCustom.module.css
```

Найдите закомментированный блок (примерно строки 62-67):

```css
/* .logoBadge {
  background: url('../../assets/Logo.png') center center / cover no-repeat;
}
.logoBadge svg {
  display: none;
} */
```

**Раскомментируйте эти строки** (уберите `/*` и `*/`):

```css
.logoBadge {
  background: url('../../assets/Logo.png') center center / cover no-repeat;
}
.logoBadge svg {
  display: none;
}
```

---

## 🔄 Шаг 3: Перезапустите сервер

После добавления файла изображения, перезапустите сервер разработки:

```bash
# Остановите сервер (Ctrl+C в терминале)
# Запустите заново:
npm run dev
```

---

## 🎯 Альтернатива: использовать существующее изображение

Если вы хотите использовать одно из существующих изображений в папке `assets`, в раскомментированном блоке замените `Logo.png` на нужное имя файла:

```css
.logoBadge {
  background: url('../../assets/biketribe-logo-mobile.png') center center / cover no-repeat;
}
```

---

## 📊 Текущие настройки логотипа

- **Позиция**: относительная (relative)
- **Размер**: 60x60px
- **Скругление углов**: 15px (border-radius)
- **Тень**: drop-shadow
- **Отображение**: cover (изображение масштабируется, чтобы заполнить всю область)

---

## ❓ Проблемы?

Если логотип не отображается корректно, проверьте:
1. ✅ Правильность пути к файлу
2. ✅ Название файла (регистр букв важен: `Logo.png`, не `logo.png`)
3. ✅ Формат файла (должен быть PNG или JPG)
4. ✅ Раскомментирован ли CSS-блок
5. ✅ Перезапущен ли сервер после добавления файла

